# https://picamera.readthedocs.io/en/release-1.13/index.html

# https://www.youtube.com/watch?v=bWwZF_zVf00

# sudo apt-get install python-picamera python3-picamera
# sudo apt-get update && sudo apt-get upgrade -y && sudo reboot
# sudo rpi-update
# # sudo reboot
# sudo apt-get install motion
# sudo modprobe bcm2835-v4l2

# https://projects-raspberry.com/live-camera-streaming-raspberry-pi-motion-setup/

# have to run this, otherwise it tries to use the init.d service, which causes issues possibly 
#sudo cp /usr/share/motion/examples/motion.service /etc/systemd/system/